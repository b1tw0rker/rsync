# Examples:
# /root/.*/
# !/root/.ssh/
#
# Notes:
# ! re-includes a later match.
# A trailing / matches directories only.

authorized_keys
/root/.*/
/root/.ssh/
/etc/fstab
/etc/default/grub
/etc/bitworker/borg
